# Simple .fxp to .bwpreset converter

Simple PHP script to convert FXP patches to bitwig presets with category and tag support.
It's using the ini file and process each instrument, which is declared there.